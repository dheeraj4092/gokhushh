# NPM Configuration
# Use pnpm instead due to npm performance issues
prefer-offline=true
progress=false

# Only install packages for current platform (skip Windows/Linux binaries)
supportedArchitectures[os]=["darwin"]
supportedArchitectures[cpu]=["x64","arm64"]
